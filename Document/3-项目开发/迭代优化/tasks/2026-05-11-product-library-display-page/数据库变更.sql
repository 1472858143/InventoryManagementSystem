-- 任务名称：商品库展示页面优化（IT-003）
-- 创建日期：2026-05-11
-- 说明：本文件为任务草稿，未经用户验收不得合并至 sql/market.sql。
--
-- 设计阶段结论：本任务【不涉及数据库结构变更】。
--
-- 原因：
--   1. 商品编号自动生成在后端 Service 层完成，product 表 product_code 列与 uk_product_code 唯一索引保留不变。
--   2. 销量字段为派生数据，通过 stock_log (change_type='OUTBOUND') 在查询时聚合，无需新增列或表。
--   3. 卡片化、筛选、排序、详情抽屉均为前端展示层改造，与表结构无关。
--
-- 如实施过程中发现仍需变更（例如希望将销量持久化为商品列以避免聚合开销），
-- 必须在此文件追加变更脚本并经用户确认，再决定是否同步更新 sql/market.sql。
