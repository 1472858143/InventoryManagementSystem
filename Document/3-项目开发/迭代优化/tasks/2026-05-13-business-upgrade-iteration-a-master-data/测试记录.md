# 测试记录

## 测试范围

- supplier / customer CRUD 与启停用主路径。
- 编码唯一冲突校验。
- 商品页分类下拉无回归。
- 旧"商品分类管理"路由不可达（redirect 到 /products）。
- 字段对账修复后既有 stock / stockLog / report 接口无退化。

## 自动化测试

| 命令 | 结果 | 说明 |
| --- | --- | --- |
| `cd backend && mvn compile -q` | ✅ 通过 | 每个后端任务完成后均验证编译通过 |
| `cd frontend && npm run build` | ✅ 通过 | 每个前端任务完成后均验证构建通过（仅 chunk size 警告，与本次无关） |
| `cd backend && mvn test` | ✅ 通过 | 2026-05-13 复测通过；含现有后端测试套件 |
| 临时启动后端后请求 `GET /api/suppliers`、`GET /api/customers` | ✅ 通过 | 补齐运行库 DDL 后均返回 `{"code":0,"message":"success","data":[]}` |

## 本次异常复测

| 场景 | 复现结果 | 修复动作 | 复测结果 |
| --- | --- | --- | --- |
| 进入供应商管理页加载列表 | `GET /api/suppliers` 返回 `{"code":500,"message":"系统内部异常"}` | 运行库补执行 IT-005 `supplier` 等 5 张表 DDL | 返回 `{"code":0,"message":"success","data":[]}` |
| 进入客户管理页加载列表 | `GET /api/customers` 返回 `{"code":500,"message":"系统内部异常"}` | 运行库补执行 IT-005 `customer` 等 5 张表 DDL | 返回 `{"code":0,"message":"success","data":[]}` |

## 手动测试

| 场景 | 操作 | 期望结果 | 实际结果 |
| --- | --- | --- | --- |
| 供应商 CRUD | 新增 / 编辑 / 启停用 / 搜索 | 全部成功，编码唯一冲突有友好提示 | 列表加载已复测通过；新增 / 编辑 / 启停用仍待用户验收 |
| 客户 CRUD | 同上 | 同上 | 列表加载已复测通过；新增 / 编辑 / 启停用仍待用户验收 |
| 商品页分类下拉 | 打开商品新增表单 | 分类下拉可加载并选择（无回归） | 待用户验收 |
| 旧分类路由下线 | 访问 `/categories` | redirect 到 `/products` | 待用户验收 |
| 字段对账回归 | 库存查询 / 库存日志 / 报表页 | 与对账前行为一致 | 待用户验收 |
| 菜单结构 | 侧边栏 | "基础资料"组含供应商/客户/商品管理，原商品分类项消失 | 待用户验收 |

## 结论

开发已完成（commits `43cb5b7` → `a392cc8`）。编译、构建与本次供应商 / 客户列表接口复测通过。新增 / 编辑 / 启停用等完整手动功能验收仍待用户执行。
