# 测试记录

## 测试范围

本任务尚未进入开发阶段。完成开发后须覆盖：

- supplier / customer CRUD 与启停用主路径。
- 编码唯一冲突校验。
- 商品页内嵌分类选择无回归。
- 旧"商品分类管理"菜单 / 路由不可达。
- 字段对账修复后既有 stock / stockLog / report 接口与页面无退化。

## 自动化测试

| 命令 | 结果 | 说明 |
| --- | --- | --- |
| `cd backend && mvn test` | 未运行 | 待开发完成后执行 |
| `cd frontend && npm run build` | 未运行 | 待前端开发完成后执行 |

## 手动测试

| 场景 | 操作 | 期望结果 | 实际结果 |
| --- | --- | --- | --- |
| 供应商 CRUD | 新增 / 编辑 / 启停用 / 搜索 | 全部成功，编码唯一冲突有友好提示 | 待执行 |
| 客户 CRUD | 同上 | 同上 | 待执行 |
| 商品页内嵌分类 | 打开商品新增表单 | 分类下拉可加载并选择 | 待执行 |
| 旧分类菜单下线 | 访问原分类路由 | redirect 或 404 | 待执行 |
| 字段对账回归 | 库存查询 / 库存日志 / 报表 | 与对账前行为一致 | 待执行 |

## 未验证项

- 全部测试项均待开发完成后执行。

## 结论

本任务当前仅完成任务目录初始化，未进入代码开发；不能声明任何业务功能完成。
