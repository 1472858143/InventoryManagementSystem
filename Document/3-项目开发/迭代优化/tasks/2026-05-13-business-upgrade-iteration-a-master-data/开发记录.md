# 开发记录

## 时间线

| 时间 | 操作 | 说明 |
| --- | --- | --- |
| 2026-05-13 | 创建任务目录 | 基于 IT-004 设计稿"迭代 A · 基础资料"段建立 IT-005 |
| 2026-05-13 | 开始开发 — 字段对账 | commit `43cb5b7` |
| 2026-05-13 | 字段对账补丁 | stock_type DEFAULT 修正，commit `cc7a034` |
| 2026-05-13 | 数据库 DDL（5 张新表） | commit `48d358f` |
| 2026-05-13 | 后端 Supplier 模块 | commit `295c68f`，修复 `fa4f7c5` |
| 2026-05-13 | 后端 Customer 模块 | commit `96ef87c` |
| 2026-05-13 | 前端菜单重组 | commit `29ee5b7` |
| 2026-05-13 | 前端 Supplier / Customer 页面 | commit `a392cc8` |

## 修改记录

| 文件 | 修改摘要 | 原因 |
| --- | --- | --- |
| `Document/3-项目开发/迭代优化/00-问题清单.md` | 新增 IT-005 条目 | 登记迭代 A 任务 |
| `sql/market.sql` + `Document/3-项目开发/market.sql` | 补 `stock.shelf_status`、`stock_log.stock_type` 字段；追加 5 张新表 | 字段对账 + 迭代 A DDL |
| `backend/.../stock/entity/Stock.java` | 补 `shelfStatus` 字段 | 与数据库对齐 |
| `backend/.../StockMapper.java` | 补 `shelf_status` 列映射 | 与数据库对齐 |
| `backend/.../StockLogMapper.xml` | 补 `stock_type` 列 INSERT | 与数据库对齐 |
| `backend/.../RestockOrder*.java` | 删除两个死代码文件 | 无调用方，清理 |
| `backend/.../supplier/` | 新增完整 supplier 模块（9 个文件） | 迭代 A 新功能 |
| `backend/.../customer/` | 新增完整 customer 模块（9 个文件） | 迭代 A 新功能 |
| `frontend/src/layout/AdminLayout.vue` | 重组菜单：新增"基础资料"分组，下线商品分类项 | 迭代 A IA 调整 |
| `frontend/src/router/index.js` | `/categories` → redirect `/products`；新增 master/suppliers、master/customers 路由 | 迭代 A IA 调整 |
| `frontend/src/api/supplier.js` | 新增（4 个函数） | 迭代 A 新功能 |
| `frontend/src/api/customer.js` | 新增（4 个函数） | 迭代 A 新功能 |
| `frontend/src/views/master/SupplierView.vue` | 新增供应商管理页面 | 迭代 A 新功能 |
| `frontend/src/views/master/CustomerView.vue` | 新增客户管理页面 | 迭代 A 新功能 |

## 决策记录

继承自 IT-004 的硬性决策见任务 README 与 `设计方案.md`。本任务内执行细节决策：

- **字段对账修复路径**：`restock_order` Mapper 与 Entity 为无调用方的死代码，直接删除（不建表）。`stock.shelf_status` 和 `stock_log.stock_type` 补列定义。
- **supplier / customer 编码生成规则**：手填优先（可留空），为空时后端自动生成。生成规则：前缀（S/C）+ yyyyMMddHHmmss + 4 位随机数。编码一旦创建不可修改。
- **启停用引用校验**：迭代 A 无引用方，本次不做校验，推迟到迭代 B 落地。

## 问题与处理

| 问题 | 处理 |
| --- | --- |
| `stock_log.stock_type` 补列时 DEFAULT 写为空字符串，与运行库不符 | 代码质量审查发现，补丁 commit `cc7a034` 修正为 `DEFAULT 'WAREHOUSE'` |
| Supplier 模块初版 SELECT 缺 `updated_at` 列，`findByCode` 未被调用，`create` 缺 `@Transactional` | 代码质量审查发现，修复 commit `fa4f7c5` 统一补齐 |

## 后续事项

- 用户验收通过后归档本任务目录到 `archive/`。
- 验收通过后开 IT-006（迭代 B：进货+销售 + StockChangeService）。
