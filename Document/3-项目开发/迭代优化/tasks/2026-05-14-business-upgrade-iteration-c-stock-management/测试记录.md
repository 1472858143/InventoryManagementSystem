# 测试记录

## 测试范围

- 报损 / 报溢创建、查询、详情和作废。
- `StockChangeService` 对 `STOCK_LOSS`、`STOCK_OVERFLOW` 的库存方向和日志来源写入。
- 报损库存不足拦截、报溢作废库存不足拦截。
- 库存报警实时查询。
- 前端 4 个库存管理页面构建和手动烟测。

## 自动化测试

| 命令 | 结果 | 说明 |
| --- | --- | --- |
| `cd backend && mvn -q test` | 未运行 | 任务刚创建，待开发后执行 |
| `cd frontend && npm run build` | 未运行 | 任务刚创建，待前端开发后执行 |

## 手动测试

| 场景 | 操作 | 期望结果 | 实际结果 |
| --- | --- | --- | --- |
| 商品报损 | 新建含多商品明细的报损单 | 保存成功，库存减少，写入 `source_type=STOCK_LOSS` | 待开发 |
| 商品报溢 | 新建含多商品明细的报溢单 | 保存成功，库存增加，写入 `source_type=STOCK_OVERFLOW` | 待开发 |
| 单据作废 | 作废报损 / 报溢单 | 状态变为 `CANCELLED`，写入反向冲销日志 | 待开发 |
| 报损报溢查询 | 按类型 / 商品 / 操作人 / 时间过滤 | 分页和详情正确 | 待开发 |
| 库存报警 | 打开库存报警页 | 低于下限或高于上限的库存被列出 | 待开发 |

## 未验证项

- 全部业务功能尚未开发，均待后续实现后验证。

## 结论

本文件为 IT-007 初始测试计划。当前仅完成任务创建，未开始功能实现，不能声明业务功能通过。
