# 测试记录

## 测试范围

待执行。本任务完成后需验证以下功能：库存拆分后的三种操作（入库/补货/出库）的库存数字变化、边界场景拒绝、分类下拉选择和外键约束、商品单位字段的存储和展示。

## 自动化测试

| 命令 | 结果 | 说明 |
| --- | --- | --- |
| `mvn test -pl backend` | 待执行 | 后端单元测试，重点 RestockService、OutboundService |
| `cd frontend && npm run build` | 待执行 | 前端编译验证 |

## 手动测试

| 场景 | 操作 | 期望结果 | 实际结果 |
| --- | --- | --- | --- |
| 入库 | 对某商品入库 10 件 | 仓库库存+10，上架库存不变 | 待验证 |
| 补货（正常） | 仓库库存 10，补货 5 | 仓库-5，上架+5，总计不变 | 待验证 |
| 补货（超量） | 仓库库存 3，补货 5 | 系统拒绝，提示"仓库库存不足" | 待验证 |
| 出库（正常） | 上架库存 8，出库 3 | 上架-3，仓库不变 | 待验证 |
| 出库（超量） | 上架库存 2，出库 5 | 系统拒绝，提示"上架库存不足，请先补货" | 待验证 |
| 新增商品 | 分类选"饮料"，单位选"瓶" | 保存成功，列表显示分类名和单位 | 待验证 |
| 商品分类 FK | 直接 API 传不存在的 categoryId | 后端返回 4xx 错误 | 待验证 |
| 盘点 | 盘点时 system_quantity = warehouse+shelf | 差值计算正确 | 待验证 |

## 未验证项

- 补货事务原子性（网络中断场景）：需要集成测试或模拟数据库连接失败
- 分类禁用后已关联商品的行为（前端下拉是否过滤禁用分类）
- 历史 stock_log 数据在新 stock_type 字段下的查询兼容性
- stock_check 盘点后仓库库存和上架库存的具体调整逻辑（总量调整还是分别调整）

## 结论

待实施后补充。不写推测性结论。
