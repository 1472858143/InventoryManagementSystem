# 测试记录

## 测试范围

- 设计阶段（2026-05-11）：仅验证任务文档结构与设计自洽性，不验证页面功能。
- 实施阶段（2026-05-12）：按 README 中 23 项验收点 + 后端单测 + 前端构建逐项验证。

## 自动化测试

| 命令 | 结果 | 说明 |
| --- | --- | --- |
| `Get-ChildItem Document\3-项目开发\迭代优化\tasks\2026-05-11-product-library-display-page` | 通过 | 已确认任务目录包含 8 个标准文件 |
| `Select-String Document\3-项目开发\迭代优化\00-问题清单.md -Pattern IT-003` | 通过 | 已确认问题清单存在 IT-003 条目 |
| `git diff --check` | 通过 | 未发现空白格式错误 |
| `cd backend && mvn -Dtest=ProductServiceImplTest test` | 先失败后通过 | 红灯：旧逻辑仍拦截空 productCode、列表无 salesCount；绿灯：4 个 ProductService 单测通过 |
| `cd backend && mvn test` | 通过 | 17 个测试通过，0 失败，0 错误 |
| `cd frontend && npm install pinyin-pro` | 通过 | 新增依赖并更新 lock 文件；npm audit 提示 1 个 moderate 既有依赖风险，未在本任务中处理 |
| `cd frontend && npm run build` | 通过 | Vite 构建通过；存在大 chunk 体积警告，非本任务新增阻断 |

## 手动测试

| 场景 | 操作 | 期望结果 | 实际结果 |
| --- | --- | --- | --- |
| 任务目录检查 | 查看 `tasks/2026-05-11-product-library-display-page/` | 包含标准任务文档 | 已包含 8 个标准任务文档 |
| 问题清单检查 | 查看 `00-问题清单.md` | 存在 IT-003 条目 | 已存在 IT-003 条目 |
| 浏览器联调 | 打开 `http://127.0.0.1:5173/products`，登录 `admin/123456` | 商品库页面可访问 | 通过，页面进入商品库卡片视图 |
| 新增商品冒烟 | 新增「联调测试商品」，不填写 productCode | 创建成功且列表出现新卡片 | 通过，后端生成 `P202605120047472007` |
| 状态切换冒烟 | 打开「可乐」详情，执行下架，再恢复上架 | 详情与卡片状态同步刷新 | 通过 |
| Console 检查 | 浏览器日志检查 error/warning | 无错误、无未处理 Promise | 无 error；修复前出现过 Element Plus radio 弃用 warning，已改为 `value` API 并重新构建 |

## 23 项验收结果

| 编号 | 结果 | 验证记录 |
| --- | --- | --- |
| 1 | 通过 | `/products` 已从 `el-table` 替换为卡片网格 |
| 2 | 通过 | 1280px 视口下每行展示 4 张卡片，卡片尺寸稳定 |
| 3 | 通过 | 卡片展示商品名称、分类和状态 |
| 4 | 通过 | 上架为绿色左边框 + 状态文字；下架状态在状态切换时已验证灰色样式生效 |
| 5 | 通过 | 点击卡片打开详情抽屉 |
| 6 | 通过 | 抽屉展示编号、名称、分类、单位、进价、售价、销量、状态、创建时间 |
| 7 | 通过 | 详情内上下架操作后，主页面卡片状态同步刷新 |
| 8 | 通过 | `ProductDetailDrawer.vue` 中以注释预留折扣、促销、关联库存、操作历史挂载点 |
| 9 | 通过 | 抽屉采用分区布局，视觉与卡片页一致 |
| 10 | 通过 | 详情以右侧 `el-drawer` 悬浮层展示 |
| 11 | 通过 | 打开和关闭详情时 URL 保持 `/products` |
| 12 | 通过 | 详情拆分为 `ProductDetailDrawer.vue` |
| 13 | 通过 | 样式沿用 `tokens.css` 变量和现有后台布局风格 |
| 14 | 通过 | 新增商品弹窗无「商品编号」字段 |
| 15 | 通过 | 不传 productCode 新增成功，列表显示自动生成编号 |
| 16 | 通过 | 搜索「饮料」可按分类匹配商品 |
| 17 | 通过 | 搜索框支持商品名称/分类模糊匹配逻辑 |
| 18 | 通过 | 售价区间控件已显示并参与本地筛选逻辑 |
| 19 | 通过 | 进价区间控件已显示并参与本地筛选逻辑 |
| 20 | 通过 | 分类多选筛选「测试分类」后仅显示对应商品；状态筛选「下架」时显示空态 |
| 21 | 通过 | 切换「售价从低到高」后列表按 2.00、3.50、15.00 顺序展示 |
| 22 | 通过 | 排序下拉包含销量、商品名 A-Z、分类 A-Z、售价升序、售价降序 |
| 23 | 通过 | 首次进入默认「销量从高到低」，销量 26 的商品排在首位 |

## 未验证项

- 未使用真实移动设备验证 320px 视口；当前通过 CSS 媒体查询支持窄屏单列与 92% 宽抽屉。
- 未清理手动验证新增的「联调测试商品」数据，当前系统无删除商品接口。

## 实施阶段验证清单（待执行）

### 后端

- [x] `cd backend && mvn test` 全部通过（含新增的 ProductService 单测）。
- [x] `POST /api/products` 不传 productCode：响应 200，返回的商品 productCode 非空且符合 `P\d{18}` 格式。
- [x] `POST /api/products` 传入 productCode：保留传入值。
- [x] `GET /api/products` 响应每项包含 `salesCount`，无出库记录的商品为 0。

### 前端

- [x] `cd frontend && npm install pinyin-pro`：pinyin-pro 安装成功。
- [x] `cd frontend && npm run build`：构建无错。
- [x] 进入 `/products` 页面：表格已替换为卡片网格。
- [x] 23 项需求验收逐条检查（参见 README 验收表）。

## 归档验证

| 命令 | 结果 | 说明 |
| --- | --- | --- |
| `cd frontend && npm run build` | 通过 | 2026-05-12 归档前复验，构建通过；仍存在大 chunk 体积警告，非阻断 |
| `cd backend && mvn test` | 通过 | 2026-05-12 归档前复验，17 个测试通过，0 失败，0 错误 |
| `git diff --check` | 通过 | 2026-05-12 归档前复验，未发现空白格式错误 |

## 结论

实施阶段（2026-05-12）已完成代码实现与验证；用户已确认任务完成，`00-问题清单.md` 已更新为「已归档」，任务目录已按工作流移动到 `archive/`。
