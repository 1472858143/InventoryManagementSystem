# 测试记录

## 测试范围

- `StockChangeService` 库存正向 / 负向变更、负库存拦截、来源日志写入。
- 进货入库、采购退货、销售出库、客户退货创建、查询、详情、作废。
- 多商品明细事务一致性和作废反向冲销。
- 前端 8 个页面的菜单、路由、表单、查询、分页和错误提示。
- 旧入库 / 出库服务回归。
- `stock_log` 新写入记录必须带 `source_type`、`source_id`、`operator_id` 和业务原因。
- `StockMapper.findEntityByProductIdForUpdate` 必须使用 `FOR UPDATE` 验证并发扣减安全。

## 自动化测试

| 命令 | 结果 | 说明 |
| --- | --- | --- |
| `cd backend && mvn test` | 未运行 | 任务刚创建，待开发后执行 |
| `cd frontend && npm run build` | 未运行 | 任务刚创建，待前端开发后执行 |

## 手动测试

| 场景 | 操作 | 期望结果 | 实际结果 |
| --- | --- | --- | --- |
| 进货入库 | 新建含多商品明细的进货单 | 保存成功，库存增加，写入 `source_type=PURCHASE_IN` 日志 | 待开发 |
| 采购退货 | 新建采购退货单 | 保存成功，库存减少，库存不足时阻止提交 | 待开发 |
| 销售出库 | 新建销售单 | 保存成功，库存减少，写入 `source_type=SALE_OUT` 日志 | 待开发 |
| 客户退货 | 新建客户退货单 | 保存成功，库存增加，写入 `source_type=SALE_RETURN_IN` 日志 | 待开发 |
| 单据作废 | 作废 4 类单据 | 状态变为 `CANCELLED`，写入反向冲销日志 | 待开发 |
| 查询筛选 | 按时间段 / 对方主体 / 单据号过滤 | 分页和详情正确 | 待开发 |
| 旧接口回归 | 调用旧入库 / 出库接口 | 旧流程仍可用，不受新业务影响 | 待开发 |

## 未验证项

- 全部业务功能尚未开发，均待后续实现后验证。

## 结论

本文件为 IT-006 初始测试计划。当前仅完成任务创建，未开始功能实现，不能声明业务功能通过。

## 2026-05-14 执行结果

### 自动化测试

| 命令 | 结果 | 说明 |
| --- | --- | --- |
| `cd backend && mvn -q "-Dtest=StockChangeServiceImplTest,StockMapperSqlTest" test` | 通过 | 验证统一库存变更入口、日志来源字段和 `FOR UPDATE` SQL。 |
| `cd backend && mvn -q "-Dtest=PurchaseServiceImplTest" test` | 通过 | 验证进货入库创建、校验、库存增加。 |
| `cd backend && mvn -q "-Dtest=PurchaseReturnServiceImplTest" test` | 通过 | 验证采购退货创建、重复商品、作废反向冲销。 |
| `cd backend && mvn -q "-Dtest=SalesServiceImplTest" test` | 通过 | 验证销售出库创建、库存不足透传、列表/详情/作废。 |
| `cd backend && mvn -q "-Dtest=SalesReturnServiceImplTest" test` | 通过 | 验证客户退货创建、列表/详情/作废和作废库存不足透传。 |
| `cd backend && mvn -q "-Dtest=PurchaseServiceImplTest,PurchaseReturnServiceImplTest,SalesServiceImplTest,SalesReturnServiceImplTest" test` | 通过 | 验证四类单据 Service 查询、详情、作废收口。 |
| `cd backend && mvn -q test` | 通过 | 后端全量测试通过；存在 Mockito 动态 agent 的 JDK 预警，不影响当前结果。 |
| `cd frontend && npm run build` | 通过 | 前端生产构建通过；存在 Vite chunk size warning，为既有体积提示。 |

### 运行库 DDL 验证

| 检查项 | 结果 |
| --- | --- |
| 连接 `jdbc:mysql://127.0.0.1:3306/supermarket_inventory` | 通过 |
| 创建 `purchase_order`、`purchase_order_item`、`purchase_return_order`、`purchase_return_order_item` | 通过 |
| 创建 `sales_order`、`sales_order_item`、`sales_return_order`、`sales_return_order_item` | 通过 |
| `stock_log` 增加 `source_type`、`source_id`、`reason`、`operator_id` | 通过 |
| `stock_log` 增加 `idx_log_source(source_type, source_id)` | 通过 |

### 接口烟测

使用当前代码临时启动 `http://127.0.0.1:18080` 后端，登录 `admin/123456` 后执行：

| 场景 | 实际结果 |
| --- | --- |
| 进货入库 | 创建 `PO202605140100038078`，列表按单据号查到 1 条，详情明细 1 条，作废成功。 |
| 采购退货 | 创建 `PRO202605140100034256`，列表按单据号查到 1 条，详情明细 1 条，作废成功。 |
| 销售出库 | 创建 `SO202605140100034776`，列表按单据号查到 1 条，详情明细 1 条，作废成功。 |
| 客户退货 | 创建 `SRO202605140100037742`，列表按单据号查到 1 条，详情明细 1 条，作废成功。 |

### 注意事项

- 首次访问 `http://127.0.0.1:8080` 返回通用 500，经验证是 8080 端口仍运行旧后端进程，未加载本轮代码；当前代码在 18080 烟测通过。
- 临时 18080 进程已停止。若继续本地联调，需要重启 8080 后端服务。
- 未执行真实浏览器可视化巡检；本轮前端验证以 `npm run build` 为准。

## 2026-05-14 结论

IT-006 后端、前端构建、运行库 DDL 和接口级最小烟测均已通过；正式 SQL 是否同步仍需用户确认后另行处理。
