# 开发记录

## 时间线

| 时间 | 操作 | 说明 |
| --- | --- | --- |
| 2026-05-13 | 创建任务目录 | 基于 IT-004 设计稿“迭代 B · 进货+销售 + StockChangeService”段建立 IT-006 |
| 2026-05-14 | 细化 IT-006 设计 | 用户确认采用完整迭代 B 边界；设计细化到数据库、库存入口、后端接口、前端页面和测试策略 |

## 修改记录

| 文件 | 修改摘要 | 原因 |
| --- | --- | --- |
| `Document/3-项目开发/迭代优化/00-问题清单.md` | 新增 IT-006 条目，IT-005 标记归档 | 登记迭代 B 并完成迭代 A 归档 |
| 本任务目录 8 个标准文件 | 填写初始目标、设计、影响分析、SQL 草案、测试计划 | 为后续开发建立上下文 |
| `设计方案.md` | 完整细化 IT-006 推荐方案、数据库、`StockChangeService`、后端接口、前端页面、测试与验收 | 进入迭代 B 设计阶段 |

## 决策记录

- IT-006 继承 IT-004 决策：主表 + 明细表、保存即生效、作废反向冲销、行锁防并发负库存、`StockChangeService` 单点入口。
- 任务总边界按完整迭代 B 建立；若开发时拆为 B1 / B2，必须在本文件补充分阶段边界，且第一阶段必须先落地 `StockChangeService`。
- 用户确认采用完整迭代 B 设计：8 张业务表、4 类单据、8 个前端页面都纳入设计；实施可分阶段。

## 问题与处理

| 问题 | 处理 |
| --- | --- |
| 迭代 B 体量较大 | 初始文档允许按 B1 / B2 分阶段提交，但不允许绕过库存统一入口 |

## 后续事项

- 开发前再次确认 Git 状态。
- 先落地数据库草案与 `StockChangeService`，再实现 4 类单据业务。

## 2026-05-14 开发实施记录

| 时间 | 操作 | 说明 |
| --- | --- | --- |
| 2026-05-14 | 落地 `StockChangeService` | 新增统一库存变更入口、来源类型、命令/结果对象；`stock_log` 写入 `source_type`、`source_id`、`reason`、`operator_id`。 |
| 2026-05-14 | 落地采购后端 | 新增进货单、采购退货单主表/明细实体、Mapper、Service、Controller；创建和作废均通过 `StockChangeService` 写库存日志。 |
| 2026-05-14 | 落地销售后端 | 新增销售单、客户退货单主表/明细实体、Mapper、Service、Controller；创建和作废均通过 `StockChangeService` 写库存日志。 |
| 2026-05-14 | 落地查询和作废收口 | 四类单据支持分页列表、单据号/主体/日期筛选、详情查询和 `NORMAL -> CANCELLED` 作废冲销。 |
| 2026-05-14 | 落地前端页面 | 新增四类 API、共享业务组件、4 个表单页、4 个查询页，并接入路由和侧边菜单。 |
| 2026-05-14 | 执行运行库 DDL | 通过 JDBC 连接 `supermarket_inventory`，创建 8 张业务表；执行前检查 `stock_log` 字段/索引是否存在，再增量添加。 |

## 本轮代码修改摘要

| 范围 | 文件/目录 | 摘要 |
| --- | --- | --- |
| 后端库存 | `backend/src/main/java/com/supermarket/inventory/stock/change/` | 新增统一库存变更服务和来源类型。 |
| 后端采购 | `backend/src/main/java/com/supermarket/inventory/purchase/` | 新增进货、采购退货 DTO/Entity/Mapper/Service/VO/Controller。 |
| 后端销售 | `backend/src/main/java/com/supermarket/inventory/sales/` | 新增销售、客户退货 DTO/Entity/Mapper/Service/VO/Controller。 |
| 后端公共 | `backend/src/main/java/com/supermarket/inventory/common/request/OrderQueryRequest.java`、`backend/src/main/java/com/supermarket/inventory/common/response/PageResponse.java` | 新增业务单据查询参数和分页响应对象。 |
| 后端认证拦截 | `backend/src/main/java/com/supermarket/inventory/config/WebMvcConfig.java` | 将 `/api/purchase/**`、`/api/sales/**` 纳入登录拦截范围。 |
| 前端 API | `frontend/src/api/purchase.js`、`purchaseReturn.js`、`sales.js`、`salesReturn.js` | 新增创建、查询、详情、作废接口封装。 |
| 前端页面 | `frontend/src/views/business/components/`、`frontend/src/views/purchase/`、`frontend/src/views/sales/` | 新增共享明细编辑、查询条、详情抽屉、汇总栏，以及 8 个业务页面。 |
| 前端路由菜单 | `frontend/src/router/index.js`、`frontend/src/layout/AdminLayout.vue` | 新增采购管理、销售管理路由和侧边菜单。 |

## 调试记录

- 首次对 `http://127.0.0.1:8080` 烟测返回通用 500。经排查，8080 端口由旧后端进程占用，未加载当前代码。
- 使用当前代码在 `18080` 端口启动临时后端后，同一进货接口创建成功，确认根因是旧进程而非本次代码或 DDL。
- 临时 `18080` 后端烟测完成后已停止；8080 旧进程仍由原环境占用，若继续本地联调需要重启后端以加载当前代码。
- 2026-05-14 用户确认该任务完成，任务状态从“待验收”改为“已完成并归档”，进入 IT-007 下一阶段。
